#include <stdio.h>
void reverse (void)
{
    char c;
    if((c=getchar())!='\n')
    reverse();
    putchar(c);
}
int main() {
   printf("enter the string to be reversed\n");
   reverse();
   return 0;
}
