#include <stdio.h>

  void main() {
  int number,count=0;
  printf("Enter the number:\n");
  scanf("%d",&number);
  while(number>0)
  {
  number = number/10;
  count++;

  }
  printf("The total number of digits in the number %d is :%d",number,count);
  }
